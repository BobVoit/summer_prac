

const Field = ({  }) => {

    const styles = {
        maxWidth: '1000px',
    }
    return (
        <div className="field" style={styles}>
            <input type="text" />
        </div>
    );
}

export default Field;